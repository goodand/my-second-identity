1) 입력 질의 x에 대해 y←""로 시작하고, 종료될 때까지 d←(반성토큰이 검색 필요라면 Retriever(x,y) 아니면 ∅) 후 (y′, r)←LM(x,y,d) 생성한다.
2) 반성토큰 r의 [Retrieve]/[NoRetrieve], [IsRel], [IsSup], [IsUse]를 점수화해 검색 재호출 여부와 후보 y′의 채택/폐기를 결정한다.
3) 채택된 최적 후보를 y에 갱신하고 종료 조건을 만족하면 최종 응답 y를 반환한다.